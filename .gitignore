# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

data/*
# …but keep the small committed samples used by CI + a fast `make up`.
!data/samples/
!data/samples/*.csv

# Per-service uv locks (builds resolve from pyproject; only the root lock is tracked)
services/*/uv.lock

# Environment / secrets
.env
.env.*
!.env.example

# Node / frontend
node_modules/
frontend/dist/

# Playwright output
frontend/test-results/
frontend/playwright-report/

# OS / editor
.DS_Store
