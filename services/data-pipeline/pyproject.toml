[project]
name = "data-pipeline"
version = "0.1.0"
description = "data-qa-agent pipeline: dbt transforms (propertyiq_staging -> staging/marts + RLS)"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.8",
    "dbt-postgres>=1.8",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
